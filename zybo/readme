ZYBOボードを用いてCPUとFPGAが通信するシステムを作成します。Linux上でFPGAを操作できるようします。

・LED4_AXI_SLAVE ディレクトリ
LEDの点滅の挙動をLinuxから操作するシステムです。

・Polling_ADD ディレクトリ
CPUから2つの引数をFPGAに与えるとFPGAがそれらの和を計算してCPUに返すというシステムです。FPGAからCPUへの結果の送信はポーリング（スピンロック）を用います。

次のやること
FPGA側のプッシュボタンが押されたことを割り込みでCPUに伝えるようなシステムの作成。
